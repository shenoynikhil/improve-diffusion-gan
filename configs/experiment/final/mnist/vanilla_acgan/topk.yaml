# @package _global_

task_name: mnist_vanilla_acgan_topk

defaults:
  - /model/diffusion_module: base.yaml
  - override /datamodule: mnist.yaml
  - override /model: vanilla_acgan_mnist.yaml

model:
  top_k_critic: ${datamodule.batch_size}
