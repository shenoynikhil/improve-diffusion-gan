output_dir: /scratch/st-jiaruid-1/shenoy/projects/cpsc533r-project/output/mnist/default

datamodule:
  data_type: mnist
  batch_size: 100

trainer:
  n_epochs: 100

model:
  network: wgan_gp
  optimizer:
    lr: 0.0002
    b1: 0.5
    b2: 0.999
