output_dir: /scratch/st-jiaruid-1/shenoy/projects/cpsc533r-project/output/cifar10/vanilla_gan

datamodule:
  _target_: src.datamodule.BaseDataModule
  data_type: cifar10
  batch_size: 100
  transforms:
    _target_: torchvision.transforms.Compose
    transforms:
      - _target_: torchvision.transforms.Resize
        size: 32
      - _target_: torchvision.transforms.ToTensor
      - _target_: torchvision.transforms.Normalize
        mean: 0.5
        std: 0.5

trainer:
  _target_: pytorch_lightning.Trainer
  accelerator: auto
  devices: 1
  default_root_dir: ${output_dir}
  max_epochs: 10
  logger: false

model:
  _target_: src.models.VanillaGAN
  lr: 0.0001
  output_dir: ${output_dir}
  generator:
    _target_: src.models.Generator
    channels: 3
    img_size: 32
    latent_dim: 100
  discriminator:
    _target_: src.models.Discriminator
    channels: 3
    img_size: 32
